version: "3.9"

services:
  sre-mvp:
    build: .
    container_name: llm-sre-multi-agent-mvp
    ports:
      - "8000:8000"
    env_file:
      - .env.example
    volumes:
      - ./:/app
